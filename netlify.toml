[build]
  command = "npm run build"
  publish="dist"
  base = "client"
  # The base directory should be the path to the nested Vue project
  [[redirects]]
  from = "/"
  to = "/index"
  status = 200
  [[redirects]]
  from = "/index"
  to = "/room"
  status = 200